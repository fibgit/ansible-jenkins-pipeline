pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Here is the build environment'
            }
        }
        
        stage('test') {
            steps {
                echo 'Here is test environment'
            }
        }
        
        stage('staging') {
            steps {
                echo 'Here is staging environment'
            }
        }
    }
}
